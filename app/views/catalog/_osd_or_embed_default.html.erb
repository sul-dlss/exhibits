<% if document.uploaded_resource? %>
  <%= render partial: "openseadragon_default", locals: {document: document} %>
<% else %>
  <%= render partial: "oembed_default", locals: {document: document} %>
<% end %>
